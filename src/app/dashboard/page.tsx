import React from "react";

const page = () => {
	return (
		<div className="flex min-h-screen w-full max-w-7xl flex-col items-center justify-between">
			private page
		</div>
	);
};

export default page;
